services:
  holdem-poker:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - holdem-data:/app/backend/data
    environment:
      - PORT=3000
      - CLIENT_URL=*
    restart: unless-stopped

volumes:
  holdem-data:
